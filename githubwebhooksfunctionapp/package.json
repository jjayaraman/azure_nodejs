{
  "name": "githubwebhooksfunctionapp",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/functions": "^4.7.0",
    "@azure/identity": "^4.8.0",
    "@azure/keyvault-secrets": "^4.9.0"
  },
  "devDependencies": {
    "@types/node": "^22.x",
    "rimraf": "^6.0.1",
    "typescript": "^5.8.2"
  },
  "main": "dist/src/{index.js,functions/*.js}"
}
